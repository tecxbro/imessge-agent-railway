# Full reference: docs/CONFIGURATION.md

# Required: PostgreSQL
# Railway supplies this from the attached PostgreSQL service.
DATABASE_URL=postgresql://agent:replace-me@localhost:5432/imessage_agent

# Optional legacy migration inputs for an existing deployment.
# Fresh deployments and local environments leave these unset and configure the
# owner through the public dashboard. Remove a legacy value manually
# only after its encrypted database migration is verified.
# OWNER_PHONE_NUMBER=+15551234567
# Former long owner alias; migration-only on Railway.
# OWNER_PHONE_NUMBER_E164_EXAMPLE_PLUS19495550123=+19495550123

# Required locally and during migration. New Railway installations can derive a
# stable UUID from RAILWAY_SERVICE_ID, but migrated installations must preserve it.
DEPLOYMENT_ID=00000000-0000-4000-8000-000000000000

# Required: Application encryption
# Generate locally with: openssl rand -base64 32
APP_ENCRYPTION_KEY=

# Codex authentication
CODEX_AUTH_MODE=chatgpt
# Required only when CODEX_AUTH_MODE=api_key.
# OPENAI_API_KEY=

# Persistent storage
# Use separate absolute paths. These directories are ignored by Git.
CODEX_HOME=/absolute/path/to/.codex-agent
AGENT_WORKSPACE_ROOT=/absolute/path/to/.agent-workspaces

# Optional/advanced: existing deployment and provider overrides
# AGENT_OWNER_HANDLES is imported only when it contains exactly one E.164 phone
# and neither owner-phone migration variable is set. Multiple handles or email
# identities require operator migration through the dashboard.
# AGENT_OWNER_HANDLES=+15551234567
# Spectrum credentials may remain unset until Photon setup stores them on the
# persistent volume.
# SPECTRUM_PROJECT_ID=
# SPECTRUM_PROJECT_SECRET=

# Optional/advanced: semantic memory
# Leave SUPERMEMORY_API_KEY empty to disable semantic memory. It is not shown
# during Railway project setup.
SUPERMEMORY_API_KEY=
SUPERMEMORY_CONTAINER_PREFIX=imessage-agent

# Runtime and safety limits
INBOUND_DEBOUNCE_MS=4000
MAX_EXECUTION_CONCURRENCY=3
MAX_OWNER_EXECUTION_CONCURRENCY=2
MESSAGE_RATE_LIMIT_PER_MINUTE=60
TASK_RATE_LIMIT_PER_HOUR=120
MAX_TASK_RUNTIME_MS=900000
PAIRING_MODE=off
GROUP_MODE=owner_mentions_only

# Retention and logging
RAW_MESSAGE_RETENTION_DAYS=30
FAILURE_RETENTION_DAYS=14
LOG_MESSAGE_CONTENT=false

# Server
PORT=10000

# Railway runtime inputs; do not set these for local development. Railway injects
# the service/deployment IDs and the volume mount variable is configured to match
# the application volume mount.
# RAILWAY_SERVICE_ID=
# RAILWAY_DEPLOYMENT_ID=
# RAILWAY_VOLUME_MOUNT_PATH=/var/data
